# 배포 방법 설명서

## 1. Google Sheets 만들기

1. 새 Google 스프레드시트를 만듭니다.
2. 메뉴에서 `확장 프로그램 > Apps Script`를 엽니다.
3. 이 폴더의 `Code.gs` 내용을 Apps Script 편집기에 붙여 넣습니다.
4. Apps Script에서 `setupSheets` 함수를 한 번 실행합니다.
5. 권한 승인 후 시트가 자동 생성되는지 확인합니다.

생성되는 시트:

- `지출DB`
- `기준정보_공종`
- `기준정보_거래처성명`
- `기준정보_지출구분`
- `기준정보_현장`

여러 현장은 `지출DB` 한 시트에 함께 저장됩니다. 각 지출 행의 `현장명` 컬럼으로 남해미조권역, 밀양하납신축, 진해사령부보수 같은 현장을 구분합니다.
앱에서 현장명을 삭제해도 `지출DB`의 과거 지출 행은 삭제되지 않습니다. `기준정보_현장` 시트의 `사용여부`만 `N`으로 변경되어 앱 선택 목록에서 제외됩니다.
현장 삭제 시 사용자가 기존 자료 삭제 처리를 선택하면 해당 현장의 `지출DB` 행은 `삭제여부 = Y`로 변경됩니다.
공종은 `기준정보_공종` 시트에서 `현장명` 컬럼으로 현장별 관리됩니다. 공종 삭제는 행 삭제가 아니라 `사용여부`를 `N`으로 변경하는 방식입니다.
입력 화면의 `작업현황 및 메모`는 `지출DB`의 `비고` 컬럼에 저장됩니다.

## 2. Apps Script 웹앱 배포

1. Apps Script 우측 상단 `배포 > 새 배포`를 선택합니다.
2. 유형은 `웹 앱`으로 선택합니다.
3. 실행 사용자는 `나`, 액세스 권한은 사용 환경에 맞게 선택합니다.
   - 현장 사용자들이 모두 Google 계정 없이 쓰려면 `모든 사용자`를 선택합니다.
   - 내부 계정만 쓰려면 조직/계정 제한을 선택합니다.
4. 배포 후 표시되는 웹앱 URL을 복사합니다.

## 3. 프론트엔드 설정

1. `index.html`, `style.css`, `script.js`를 같은 폴더에 둡니다.
2. 브라우저에서 `index.html`을 엽니다.
3. 하단 `설정` 탭의 `Google Apps Script 웹앱 URL`에 복사한 URL을 입력합니다.
4. `설정 저장` 후 상단 동기화 버튼을 누릅니다.

원하면 `script.js` 상단의 `GAS_WEB_APP_URL`에 URL을 직접 넣어 기본값으로 사용할 수 있습니다.
정적 사이트에서 CORS가 막히는 경우를 대비해 앱은 `fetch` 실패 시 자동으로 JSONP 방식으로 재시도합니다.

## 4. GitHub Pages 배포

1. GitHub 저장소에 `index.html`, `style.css`, `script.js`를 올립니다.
2. 저장소 `Settings > Pages`에서 배포 브랜치와 폴더를 선택합니다.
3. 발급된 Pages URL로 접속합니다.
4. 최초 1회 설정 탭에서 Apps Script URL을 저장합니다.

## 5. PDF 출력 참고

PDF 출력은 브라우저 인쇄 기능을 사용합니다. `PDF 출력` 버튼을 누른 뒤 인쇄 대상에서 `PDF로 저장`을 선택하면 한글이 깨지지 않는 정산서를 만들 수 있습니다.
