# 사용설명서

## 빠른 입력

1. 하단 `입력` 탭을 엽니다.
2. 상단에서 현재 현장을 선택합니다.
3. 지출일자, 지출구분, 공종, 업체명/성명, 금액을 입력합니다.
4. `저장`을 누릅니다.
5. 저장 후 입력칸이 바로 초기화되어 다음 지출을 연속 입력할 수 있습니다.

현장명은 필수입니다. 하나의 Google Sheets `지출DB`에 여러 현장의 지출을 함께 저장하고, 상단 현장 선택값으로 대시보드·조회·정산서를 구분합니다.

## 현장 관리

- 화면 최상단 `현장 선택` 영역에서 현장명을 선택, 추가, 삭제하고 Google Sheets의 현재 현장 자료를 다시 불러올 수 있습니다.
- `현장 추가`를 누르면 추가 입력칸이 열립니다. 새 현장명을 입력하고 `현장 추가`를 다시 누르면 드롭박스에 바로 표시됩니다.
- 삭제할 현장을 선택한 뒤 `현장 삭제`를 누르면 선택 목록에서만 빠지고, 기존 `지출DB` 내역은 삭제되지 않습니다.
- 현장 삭제 시 기존 지출DB 자료까지 삭제 처리할지 한 번 더 묻습니다. `확인`을 누르면 기존 자료도 `삭제여부 = Y`가 되어 조회에서 제외되고, `취소`를 누르면 현장 목록에서만 제외됩니다.
- `불러오기`는 Google Sheets에 저장된 최신 지출, 공종, 거래처/성명, 현장 기준정보를 다시 가져온 뒤 현재 선택 현장 기준으로 화면에 표시하는 버튼입니다.
- 삭제된 현장은 `기준정보_현장` 시트에서 `사용여부`가 `N`으로 바뀝니다.
- 같은 현장명을 다시 추가하면 `사용여부`가 `Y`로 바뀌어 다시 사용할 수 있습니다.

금액은 숫자만 입력하면 자동으로 천 단위 콤마가 표시됩니다. 부가세는 부가세 제외 금액의 10%로 계산되고, 합계금액은 부가세 포함 금액으로 저장됩니다.

## 공종 관리

- 공종은 현재 선택된 현장별로 관리됩니다.
- 입력 화면에서 공종 옆 `추가`를 누르면 공종 추가 입력칸이 열립니다.
- 새 공종명을 입력하고 `공종 추가`를 누르면 현재 현장의 공종 기준정보에 저장되고 공종 드롭박스에 바로 표시됩니다.
- 공종 입력칸에 새 공종명을 먼저 적은 상태에서 `추가`를 누르면 즉시 저장됩니다.
- 공종 입력칸에서 삭제할 공종을 선택하거나 입력한 뒤 `삭제`를 누르면 현재 현장 드롭박스에서 제외됩니다.
- `설정` 탭의 `현재 현장 공종`에서도 공종을 추가하거나 삭제할 수 있습니다.
- 삭제된 공종의 기존 지출내역은 삭제되지 않고, 기준정보의 `사용여부`만 `N`으로 바뀝니다.

## 작업현황 및 메모

- 입력 화면의 `작업현황 및 메모`에 현장 기록을 남길 수 있습니다.
- 예: `미장공사 3명`, `레미콘 타설`, `자재 반입`.
- 이 내용은 Google Sheets `지출DB`의 `비고` 컬럼에 저장됩니다.
- 저장된 내역을 수정하면 `작업현황 및 메모`도 함께 수정됩니다.

## 업체명/성명 자동완성

- 한 번 저장한 업체명 또는 성명은 자동으로 기준정보에 저장됩니다.
- 노무비 입력 시 성명 검색, 외주비 입력 시 업체명 검색에 활용됩니다.

## 대시보드

하단 `대시보드` 탭에서 상단에 선택된 현장 기준으로 전체, 월별, 주간별, 일자별 금액을 확인합니다. 상단 현장을 `전체 현장`으로 바꾸면 모든 현장의 합계를 볼 수 있습니다.

- 총 노무비
- 총 외주비
- 총 자재비
- 총 경비
- 전체 합계

주간은 월요일부터 일요일 기준입니다.

## 조회

하단 `조회` 탭에서 상단에 선택된 현장 기준으로 다음 조건을 조합해 검색할 수 있습니다.

- 기간: 전체, 월별, 주간별, 일자별, 직접 선택
- 지출구분
- 공종
- 업체명/성명
- 금액 범위

조회 결과의 각 항목에서 `수정` 또는 `삭제`를 누를 수 있습니다. 삭제는 완전 삭제가 아니라 `삭제여부`를 `Y`로 바꾸는 방식이며, 일반 조회와 정산서에서는 제외됩니다.

## 전용 조회

조회 탭 하단에서 전용 조회를 사용할 수 있습니다.

- 공종별: 선택한 공종의 노무비, 외주비, 자재비, 경비, 합계를 확인합니다.
- 업체별 외주비: 업체명 기준으로 외주비 총액, 월별, 주간별, 상세내역을 확인합니다.
- 성명별 노무비: 작업자 성명 기준으로 노무비 총액, 월별, 주간별, 상세내역을 확인합니다.

## 정산서 PDF

하단 `정산서` 탭에서 양식을 선택하고 `PDF 출력`을 누릅니다.
정산서는 상단에 선택된 현장 기준으로 출력됩니다. `전체 현장`을 선택하면 모든 현장 내역을 합산해 출력합니다.

지원 양식:

- 전체 지출내역서
- 월별 정산서
- 주간 정산서
- 일자별 지출내역서
- 공종별 지출내역서
- 업체별 정산서
- 성명별 노무비 내역서

PDF에는 현장명, 기간, 작성일, 작성자, 구분별 합계, 전체 합계, 상세내역 표가 포함됩니다.

## 동기화

- Apps Script URL이 설정되어 있으면 저장, 수정, 삭제가 Google Sheets에 반영됩니다.
- 네트워크 문제로 서버 저장이 실패해도 브라우저 로컬에는 먼저 저장됩니다.
- 상단 동기화 버튼을 누르면 Google Sheets 데이터를 다시 불러옵니다.
